---
interface Props {
  text: string;
}

const { text } = Astro.props;
---

<p class="flex items-center gap-2 text-xs font-bold uppercase text-foreground">
  <span class="size-1.5 rounded-full bg-primary"></span>
  {text}
</p>
